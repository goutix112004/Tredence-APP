# Tredence-APP
A minimal FastAPI-based workflow engine that executes a graph of Python function “nodes” over shared state, with support for branching, looping, and a sample code-review agent workflow as required in the assignment.
